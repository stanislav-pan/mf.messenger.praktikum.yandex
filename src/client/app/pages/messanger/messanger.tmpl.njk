{% if modalComponentId %}
    <ng-container data-componentsids="{{modalComponentId}}"></ng-container>
{% endif %}

<div class="messanger">
    <aside class="messanger__left">
        <header class="messanger__left-header">
            <button class="btn-icon messanger__profile-btn" onclick="goToSettings">
                <span class="messanger__profile-btn-span">Profile</span>
                {% include 'static/icons/keyboard-arrow-right.tmpl.njk' %}
            </button>

            <div class="messanger__search">
                <ng-container data-componentsids="{{searchComponentId}}"></ng-container>
            </div>
        </header>

        <div class="create-chat" onclick="createChat">
            Create chat
        </div>
        <ng-container data-componentsids="{{chatsCopmonentId}}"></ng-container>
    </aside>
    <main class="messanger__chat">
        {% if currectChatId %}
            <header class="messanger__chat-header">
                <div class="messanger__chat-header-brief-information">
                    <ng-container data-componentsids="{{briefInformationComponentId}}"></ng-container>
                </div>

                <button class="messanger__chat-action-btn btn-icon" onclick="showChatMenu">
                    {% include 'static/icons/more-settings.tmpl.njk' %}
                </button>

                {% if chatMenuComponentId %}
                    <ng-container data-componentsids="{{chatMenuComponentId}}"></ng-container>
                {% endif %}
            </header>
        {% endif %}

        <div class="messanger__chat-main">
            {% if currectChatId %}
                <ng-container data-componentsids="{{messagesComponentId}}"></ng-container>
            {% endif %}
        </div>

        {% if currectChatId %}
            <footer class="messanger__chat-footer">
                <button class="btn-icon messanger__chat-footer-attach-btn">
                    {% include 'static/icons/attach.tmpl.njk' %}
                </button>

                <input type="text" class="messanger__chat-input" placeholder="Message">

                <button class="btn-icon messanger__chat-footer-send-btn">
                    <div class="badge">
                        {% include 'static/icons/send.tmpl.njk' %}
                    </div>
                </button>
            </footer>
        {% endif %}
    </main>
</div>